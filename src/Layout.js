var LayoutNode = require( './LayoutNode' );


var Layout = function( layoutFunction ) {

	this.layoutFunction = layoutFunction;
};

Layout.prototype.layoutFunction = null;

Layout.prototype.create = function( itemToLayout ) {

	
};

Layout.prototype.resizeAndPosition = function( x, y, width, height ) {

	
};



module.exports = Layout;